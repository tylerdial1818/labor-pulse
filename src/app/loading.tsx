import { LoadingState } from "@/components/states/loading-state";

export default function Loading() {
  return <LoadingState title="Preparing dashboard" description="Loading the latest analytical view." />;
}
